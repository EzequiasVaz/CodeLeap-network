import { styled } from "../../styles/stitches.config"


export const LoginPageContainerFluid = styled('div', {
    width: '100%',
    height: '100vh',
    display: 'flex',
    alignItems: 'center',
    justifyContent: 'center',
})